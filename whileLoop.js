// Números
var count = 1;

while(count <= 100) {
    console.log(count);
    count++;
}

// Carateres de uma string
var string = "Hello!";

var count = 0;                      // O primeiro carater é o index 0

while (count < string.length) {
    console.log(string[count]);
    count++;
}